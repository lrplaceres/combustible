{% extends 'backend.html.twig' %}

{% block title %}Editar Lugar
{% endblock %}

{% block article %}
	<h1 class="text-center back-white">Editar Lugar</h1>

	<div class="back-white">
		{{ include('lugar/_form.html.twig', {'button_label': 'Aceptar'}) }}
	</div>

	<div class="btn-group">
		<a href="{{ path('app_lugar_index') }}" class="btn btn-secondary"><i class="ti ti-square-arrow-left"></i> Lugares</a>
		{{ include('lugar/_delete_form.html.twig') }}
	</div>
{% endblock %}
