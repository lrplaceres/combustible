{% extends 'backend.html.twig' %}

{% block title %}Tipo{% endblock %}

{% block article %}
    <h1 class="text-center back-white">Tipo</h1>

    <table class="table table-striped table-hover back-white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo.nombre }}</td>
            </tr>
        </tbody>
    </table>

	<div class="btn-group">
    <a href="{{ path('app_tipo_index') }}" class="btn btn-secondary"><i class="ti ti-square-arrow-left"></i> Tipo</a>

    <a href="{{ path('app_tipo_edit', {'id': tipo.id}) }}" class="btn btn-primary"><i class="ti ti-edit"></i> Editar</a>

    {{ include('tipo/_delete_form.html.twig') }}

    	</div>
{% endblock %}
