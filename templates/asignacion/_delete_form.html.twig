<form method="post" action="{{ path('app_asignacion_delete', {'id': asignacion.id}) }}" onsubmit="return confirm('¿Desea eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asignacion.id) }}">
    <button class="btn btn-danger"><i class="ti ti-trash"></i> Eliminar</button>
</form>
