<form method="post" action="{{ path('app_empresa_delete', {'id': empresa.id}) }}" onsubmit="return confirm('¿Desea eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ empresa.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
