{% extends 'backend.html.twig' %}

{% block title %}Editar Entidad
{% endblock %}

{% block article %}
	<h1 class="text-center back-white">Editar Entidad</h1>

	<div class="back-white">
		{{ include('empresa/_form.html.twig', {'button_label': 'Aceptar'}) }}
	</div>

	<div class="btn-group">
		<a href="{{ path('app_empresa_index') }}" class="btn btn-secondary"><i class="ti ti-square-arrow-left"></i> Entidades</a>
		{{ include('empresa/_delete_form.html.twig') }}
	</div>

{% endblock %}
